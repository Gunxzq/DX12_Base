# 反射系统管理器
1. 颜色纹理数组
2. 每个反射探头6次（GS可以一次）
3. 根据级别来更新
4. 视锥剔除，可见集复用
5. 动态调整分辨率和更新频率


## 更新调度
P0	动态反射	每帧	1024x1024	主角车辆、镜子
P1	场景探头	每3-5帧	512x512	重要静态物体
P2	环境探头	每秒1次	256x256	远景、背景
P3	静态烘焙	一次	128x128	固定场景


PrePass 阶段
    │
    ├── 任务1：阴影贴图捕获（方向光级联 + 点光源六面）
    │
    ├── 任务2：反射捕获（动态立方体贴图）
    │
    └── 任务3：深度预渲染（可选）



## 动态反射探针、静态光照探针 
|维度|动态反射探针|静态光照探针|
|---|---|---|
|核心目的|捕获实时的镜面反射（例如：金属球里映出的动态人影）|存储预计算的漫反射和镜面反射光照（用于给动态物体打光）|
|更新频率|运行时高频（每帧、或每N帧更新）|离线烘焙（构建时一次）|
|数据内容|实时渲染的场景（带当前动画、动态物体）|预计算的光照信息（不含动态物体）|
|位置用途|作为相机的放置点，从该点观察周围|作为数据的存储点，供周围物体插值采样|
|Shader使用	|直接采样该点的Cubemap，无需插值	|插值周围多个探针的数据，计算本点的光照|


1. 反射探针：相机点、捕获范围、Cubemap纹理句柄
2. 光照探针：采样点、烘培出的光照数据、影响范围


## 反射与光照

### 屏幕空间反射 (SSR)
原理：复用主相机渲染好的画面，通过光线步进计算屏幕空间里物体的反射。
性能：低（后期处理阶段的一两个Pass）。
优势：性能好，反映的是当前帧画面，任何东西都能被反射（包括动态物体、粒子）。
缺陷：只能反射屏幕内看到的东西；物体边缘会有“截断”；无法反射被遮挡的物体。
定位：最常用的反射技术，适合粗糙、模糊的反射。


### 动态反射探针 (Dynamic Reflection Probe)
原理：就是我们正在讨论的，从特定点实时渲染周围环境到Cubemap。
性能：高（每探针每帧6次渲染）。因此一般只给最重要的物体（如主角车辆）使用。
优势：能反射屏幕外的物体，没有SSR的截断问题。
缺陷：从探针点采样导致位置误差；开销巨大，无法大规模使用。
定位：为高优先级物体提供“精准但昂贵”的反射。


### 静态反射探针 (Static Reflection Probe)
原理：离线烘焙的Cubemap，只包含静态世界的反射信息。
性能：极低（几乎无运行时开销，只是一个纹理采样）。
优势：免费，质量可控。
缺陷：烘焙的是静态场景，看不到任何动态物体。移动物体会“消失”在反射里，产生穿帮。
定位：作为全局或备用反射，为所有物体提供“最低保证”的环境反射。



### 光照探针 (Light Probe) / 烘焙光照贴图
原理：存储的是预计算的光照数据，不是反射图像。动态物体会在周围几个探针间插值，从而获得与场景匹配的光照。
性能：极低（插值计算 + 查找球谐系数/纹理）。
优势：让动态物体“融入”场景光影。
缺陷：无法表达高频的镜面反射，只有低频的漫反射光照。
定位：解决动态物体的光照一致性问题，而非提供反射图像。



## 使用场景
|材质类型|需要的反射质量|是否用动态反射|示例|
|---|---|---|---|
|粗糙材质|不需要|❌|布料、砖墙、泥土|
|低光泽材质|模糊反射|❌（静态足矣）|木材、塑料、磨砂金属|
|金属材质	|清晰反射|⚠️ 可选|拉丝金属、铜器|
|高光材质	|镜面反射|✅ 强烈建议|铬金属、镜子、水面|
|特殊物体	|精准反射|✅ 必须|主角车辆、互动物品|



|方案|质量|开销|适用场景|
|---|---|---|---|
|静态 Cubemap|低（无动态物体）|	几乎为零	|粗糙材质、远景、保底|
|屏幕空间反射	|中（仅屏幕内）|	低（1个Pass）	|地面、水面、中等光滑材质|
|动态反射探针	|高（全场景）|	极高（6次渲染）	|主角车辆、镜子、关键物品|




## 相机模式、盒体模式

### 相机模式
1. 采样视角，只包含采样的方向，不包含位置，结果是一张无限远的背景画
2. 观察者移动的话，反射的结果不变，因为探针是固定的3
3. 直接根据反射方向采样



### 虚拟盒体模式
1. 采样视角，但是在一个有限的盒子空间内部，需要计算位置
2. 这个虚拟盒子是数学边界，不存在真实的mesh来表达
3. 计算视线与虚拟盒子的交点，用交点方向采样，模拟视差
4. 虚拟盒子参与着色器计算，修正采样的方向





