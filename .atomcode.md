# 项目上下文与约束规则



## 项目架构特点
1. 渲染与特效PASS：基于ECS组件-渲染项构建器-渲染器的流程
2. 多线程安全性，system中不能进行内存分配，渲染的system只能录制命令
3. system 调度：高频连续过程走 AlwaysRun 常驻 System，低频离散事件走 WithMessage 消息触发。禁止用消息模拟每帧驱动
4. 全屏 Quad / 后处理 PASS（非主渲染）：必须在 Draw 前显式设置视口（RSSetViewports）和裁剪矩形（RSSetScissorRects），因为新分配的命令列表无默认视口
5. 全屏 Quad 渲染的 PSO 须设置 `CullMode = NONE`（SV_VertexID 生成的三角形绕序为逆时针）
6. 所有渲染器的根签名须完整声明着色器使用的静态采样器，否则 PSO 创建失败且无编译错误提示
7. 渲染器的 PSO 顶点输入布局（`D3D12_INPUT_ELEMENT_DESC`）必须与网格数据源的实际存储格式完全一致，特别留意骨骼 `BLENDINDICES` 使用 `R8G8B8A8_UINT` 而非 `R32G32B32A32_UINT`、以及各字段的 `AlignedByteOffset` 与实际顶点结构体的字节偏移匹配
8. 资源管理器/池（`RenderTargetPool`、`GpuResourceManager` 等）只负责资源生命周期（分配/释放），不追踪也不重置 GPU resource state。使用资源的 system 必须在自己管理的命令列表中通过 `ResourceBarrier` 将资源转到所需状态，**不能假设初始状态为 `COMMON`**
9. `OnResize`/重建资源的方法必须检查尺寸是否真实变化，避免窗口初始化过程中重复重建导致池化资源残留错误 GPU 状态
10. 所有管理 GPU ResourceBarrier 的 System 必须遵循 **初始状态→所需状态→渲染→初始状态** 的对称屏障规则：每个 System 在自己录制的命令列表中完成入口转换后，结束时必须做反向转换恢复到入口前的状态。禁止仅做入口转换而遗漏出口转换。此规则适用于所有资源（共享 RT、私有 RT、深度缓冲等），不因资源是否被复用而豁免
11. 资源管理器（`GeometryResourceManager`、`TextureManager`、`MaterialManager` 等）与 `GpuResourceManager` 是**协作模式**，而非包含/依赖关系。资源管理器只管理自己的槽位/索引/引用计数，**禁止直接调用 `GpuResourceManager::Release`**。GPU 资源的实际释放由 `GpuResourceManager` 统一管理，通过 `GpuWorkItem::uploadBufferHandles` 机制或 `GpuResourceManager::Update` 的 fence 回调完成。资源管理器只需在 `Release`/`Reclaim` 中释放自己的槽位即可
12. `CD3DX12_ROOT_SIGNATURE_DESC` 必须使用 `Init()` 方法初始化，禁止手动赋值 `NumParameters`/`pParameters`/`Flags` 等字段。`{}` 零初始化后手动赋值会导致 `D3D12SerializeRootSignature` 内部触发 `std::bad_alloc`（原因是 `NumStaticSamplers` 和 `pStaticSamplers` 等字段未正确初始化，导致函数尝试分配超大内存）。同样，`CD3DX12_ROOT_PARAMETER` 必须显式设置 `ShaderVisibility` 字段

13. 所有从 C++ 端传入的 `XMMATRIX` 矩阵常量，在 HLSL 中必须声明为 `row_major float4x4`。`float4x4`（默认 column_major）与 C++ 端 `XMMATRIX` 的行主序存储不匹配，导致 `mul` 变换结果错误。非矩阵类型（`float4` 等）不能加 `row_major`/`column_major` 修饰，否则编译错误 `X3077`

14. **编辑器初始化顺序约束**：`Editor::Initialize()` 中，各模块的初始化顺序必须严格遵循以下规则：
    - **非 Default 堆域的堆分区必须在异步加载和主循环开始前注册完毕**。具体来说，使用 `HeapTag::EditorViewport` 的 `AddPartition`（特别是 `CBV_SRV_UAV / Texture` 分区）必须在 `EditorScene::LoadDefaultScene()`（异步加载）和 `EditorViewport::Initialize()`（主循环）之前完成。
    - 正确顺序：`SkyboxManager::Initialize(heapTag)` → `AddPartition(CBV_SRV_UAV, Texture, ..., EditorViewport)` → `EditorScene::LoadDefaultScene()` → `EditorViewport::Initialize()`
    - 违反此规则会导致 `SkyboxManager::CreateCubeSRV` 中的 `Allocate(heapTag, PartitionType::Texture)` 因分区不存在而返回 `UINT32_MAX`，`SkyboxManager::IsValid()` 永远为 `false`，天空盒等依赖该堆域资源的渲染不生效。
    - `EditorViewport::CreateRenderTarget` 中只添加 RTV/DSV 分区，不允许重复添加已在 `Editor::Initialize()` 中注册过的 `CBV_SRV_UAV / Texture` 分区，否则 `OnResize` 触发重复注册会产生 `reservedStart != baseOffset` 警告。

15. **编辑器主循环必须驱动 BackgroundExecutor**：`Editor::Run()` 的主循环中，必须在 `FrameDriver::Tick()` 之前调用 `BackgroundExecutor::Tick()`。Game 端在 `GameWorld::Update()` 中执行此调用，Editor 端容易遗漏。缺少此调用会导致：
    - 异步加载任务（`AssetManager::LoadBatch`）完成后，`onAllComplete` 回调不触发
    - `SceneConstructor::OnDependenciesLoaded` 不执行
    - `SkyboxManager::SetSkybox` 不被调用，`IsValid()` 永远返回 `false`
    - 所有依赖异步加载的资源（天空盒纹理、网格等）均不可用
    - 正确顺序：`MainTimer::Tick()` → `BackgroundExecutor::Tick()` → `FrameDriver::Tick()`

16. **编辑器离屏 RT 的深度格式必须与主交换链一致**：`EditorViewport::CreateRenderTarget` 中创建深度缓冲时，禁止硬编码深度格式（如 `D24_UNORM_S8_UINT`）。必须使用 `m_context->DeviceContext->GetDepthStencilFormat()` 获取主交换链的深度格式。原因：
    - `SkyRenderer`、`GridRenderer` 等渲染器的 PSO 的 `DSVFormat` 来自 `m_context->GetDepthStencilFormat()`
    - 若离屏 RT 的深度格式与 PSO 的 `DSVFormat` 不匹配，D3D12 报 `ERROR #615: DEPTH_STENCIL_FORMAT_MISMATCH_PIPELINE_STATE`
    - 深度格式在 `renderer.json` 中配置（`depthStencilFormat` 字段），Editor 和 Game 使用同一配置
    - 同理，`DSV` 描述符创建（`D3D12_DEPTH_STENCIL_VIEW_DESC::Format`）也必须使用同一格式值

17. **编辑器多堆模式下 `GetPartitionCpuHandle` 必须显式传入 `HeapTag`**：在编辑器多堆模式（`HeapMode::Multi`）下，所有通过 `DescriptorHeapCollection` 获取 CPU/GPU 描述符 handle 的操作必须显式指定 `HeapTag`，禁止依赖 `HeapTag::Default` 默认参数。原因：
    - 编辑器视口渲染使用 `HeapTag::EditorViewport` 的独立 CBV_SRV_UAV 堆，与 `Default` 堆物理隔离
    - CPU handle（`GetPartitionCpuHandle`）和 GPU handle（`GetPartitionGpuHandle`）必须指向**同一个堆**，否则 `CreateShaderResourceView` 将 SRV 写入 `Default` 堆，而 `SetGraphicsRootDescriptorTable` 绑定的 GPU handle 指向 `EditorViewport` 堆的未初始化槽位，导致 D3D12 GPU-Based Validation 报 `Uninitialized descriptor accessed` 错误
    - Game 端使用 `HeapTag::Default`，`GetPartitionCpuHandle` 的默认参数恰好匹配，不会暴露此问题；编辑器多堆模式下严格传参才能避免
    - 典型错误：`m_descHeaps->GetPartitionCpuHandle(PartitionType::Texture, index)` 缺 `m_heapTag` 参数

18. **编辑器新增面板时必须同步更新语言包**：所有通过 `ImGui::Begin()` 创建的新窗口，其标题必须使用 `EditorStrings::Get("key", "fallback")` 获取，并在 `Editor/Config/editor_strings_*.json` 中添加对应的 key 和所有语言的翻译。同时 `EditorLayout::InitializeDockLayout()` 中的 `DockBuilderDockWindow` 调用也必须使用与 `ImGui::Begin` 完全一致的窗口名（含 `###ID` 后缀）。违反此规则会导致：
    - 新窗口标题无法随语言切换
    - Dock 布局无法正确匹配窗口（`DockBuilderDockWindow` 找不到同名窗口）
    - 语言包缺失时回退到 fallback 英文，不会崩溃

19. **资产体系：原子资产（三元组）与复合资产**：
    - **原子资产（三元组）**：`Mesh`（`GeometryResourceManager`）、`Material`（`MaterialManager`）、`Texture`（`TextureManager` + `GpuResourceManager`）是运行时的最小可独立加载单元，每个原子资产对应一个运行时系统。
    - **复合资产**：`Scene`、`Terrain`、`ParticleSystem` 等通过 JSON 描述组合原子资产，不直接持有 GPU 数据，只持有对原子资产的 Handle 引用。
    - **加载时序**：复合资产的依赖（原子资产）必须先于复合资产自身加载完成。见 `Docs/architecture/AssetSpecification.md`。
    - **几何子类型**（`TriangleMesh`、`SkinCluster`、`PatchMesh` 等）不提升到 `AssetType`，是 `GeometryResourceManager` 的内部子类型，由 `.dxmesh` 文件内部的数据段决定。
    - **`AssetType` 枚举**独立定义于 `Engine/Asset/Definitions/AssetType.h`，新增类型时需同步更新 `AssetManager::Load()` 的 switch 分支、`FileIconProvider` 的扩展名映射、以及 `SceneConstructor` 的依赖收集逻辑。详见 `Docs/architecture/AssetTypeDefinition.md`。

20. **命名空间使用规范**：引擎 CORE 的使用者应尽可能使用 `using namespace` 指令，避免全限定名。具体模式：
    - 在 `.cpp` 文件顶部添加 `using namespace DX12Engine;` 及所需的子命名空间（如 `using namespace DX12Engine::Renderer;`）
    - 头文件中禁止使用 `using namespace`（避免污染引用者的命名空间），使用前向声明或全限定名
    - 完整示例见 `Editor/EditorLib/Editor.cpp` 第 34-41 行

21. **编辑器 Panel 体系与 EditorLayout 职责**：
    - `EditorLayout` 的职责仅限于：注册 RootDrawCallback、定义 Dock 布局拆分、遍历已注册的 `IEditorPanel` 调用其 `Draw(float)`。**禁止持有具体面板的成员变量或转发方法**。
    - 所有编辑器面板必须继承 `IEditorPanel` 接口，实现 `GetWindowName()`、`Draw(float)`、`IsVisible()`/`SetVisible()`、`GetTargetDockId()` 等虚方法。面板自行管理显隐标志（通过 `ImGui::Begin` 的 `p_open` 参数），EditorLayout 不管理任何面板的显隐。
    - `Editor` 直接持有各面板对象（值成员或 `unique_ptr`），在 `Initialize()` 中调用 `m_layout->RegisterPanel(&panel)` 注册。面板间的依赖（如 AssetBrowser 需要预览管理器）由 Editor 直接注入，**不再通过 EditorLayout 转发**。
    - `Draw(float deltaTime)` 实现必须遵守 Panel Draw 规范：早期返回 → 消费外部数据 → `ImGui::Begin(GetWindowName(), &m_visible)` → 工具栏/筛选栏 → 分隔线 → 主体内容 → `ImGui::End()`。窗口名与 `GetWindowName()` 一致（含 `###ID` 后缀），标题使用 `EditorStrings::Get(key, fallback)` 支持多语言。
    - Console 等复杂面板独立为 Panel，自己持有 spdlog sink 和筛选状态（关键字搜索、级别过滤、类型过滤），不再由 EditorLayout 托管。
    - 新增面板时需同时更新语言包（`editor_strings_*.json`），并确保 `GetTargetDockId()` 返回的 Dock ID 在 `EditorLayout::InitializeDockLayout()` 中已定义。
    - 详见 `Docs/architecture/EditorPanelSystem.md`。

> 文档索引见 `Docs/README.md`，包含架构设计、Bug 修复记录、开发笔记、待办清单等



## 沟通与响应要求
- 沟通以讨论为主，当包含疑问的命令是不可以执行文件的修改的
- 明确的命令，才可以执行具体的操作
- 方案讨论为主，不可以直接执行操作
- 不要使用ps1脚本，如果源文件较大，可以写入一个临时的文件，人工手动复制修改对应的位置
- 不可以执行编译命令，编译由人工手动执行
- **禁止自动 Git 提交**：所有代码改动的提交必须由人工手动执行，AI 不允许自动调用 `git commit`、`git push` 或任何形式的自动版本控制操作