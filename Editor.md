# 编辑器

## 1. 概述

引擎分为两种运行模式，通过编译宏 `#if WITH_EDITOR` 隔离：

| 模式 | 宏 | 核心目标 |
|------|-----|---------|
| **编辑器（开发）模式** | `WITH_EDITOR` | 稳定性、热重载、调试友好（30fps 稳定即可） |
| **发布（Runtime）模式** | 无 | 极致性能，榨干硬件 |

两种模式必须共用同一套 Root Signature 和着色器。多堆模式应模拟大堆的索引方式，只改变堆的创建和分配逻辑，避免维护两套渲染管线代码。

---

## 2. 描述符堆策略

### 2.1 编辑器模式：多堆策略

每个模块或 Pass 拥有独立的描述符堆，原因：

| 优势 | 说明 |
|------|------|
| **崩溃隔离** | 编辑器频繁加载/卸载资产、热重载 Shader。单堆下一个错误的描述符越界可能污染整个大堆；多堆可将问题限制在单个堆内，不会拖垮整个编辑器 |
| **宽容的内存管理** | 多堆可按需创建和销毁，避免单堆下复杂的 Free List 和分代索引逻辑 |
| **清晰的调试信息** | 在 RenderDoc/PIX 中，每堆有明确的命名和用途（如 `Editor_SelectionHighlight_Heap`），过滤定位方便 |
| **热重载支持** | 只重建受影响的 Pass 堆，无需重置整个全局大堆 |

### 2.2 运行时模式：单堆 + 环形分配器

发布版没有热重载需求，生命周期确定：

- 资源在关卡加载时一次性创建，卸载时一次性销毁
- 不需要原子切换（新旧堆指针切换）
- 不需要延迟回收（无需等待 GPU 多帧）
- 使用环形分配器，维护 `AllocPtr` 和 `FreePtr`，每帧重置指针实现逻辑清空
- 零外部碎片，完美匹配临时数据的 1-3 帧生命周期

### 2.3 分配策略对比

| 场景 | 堆策略 | 分配策略 | 核心目标 |
|------|--------|----------|----------|
| 编辑器（多堆） | 每个模块或 Pass 独立堆 | 线性分配 或 复用优先 | 可靠性：隔离崩溃，方便热重载 |
| 运行时（单堆） | 全局唯一大堆 | 环形分配（Ring Buffer） | 性能：无碎片，O(1) 分配释放 |

---

## 3. 多进程架构

游戏程序与编辑器拆分为不同进程，通过数据驱动（如 JSON、二进制资源包）作为桥梁——编辑器不直接操作渲染进程的内存，渲染进程监听文件变化，自主加载新资源。

### 3.1 热重载的原子替换流程

```
修改材质并保存 → 数据驱动文件更新 → 游戏进程监听到变化
    ↓
1. 新建独立堆，加载新资源
2. 单帧内，将模型渲染指令从指向旧堆原子地切换到指向新堆
3. 标记旧堆为"待回收"，延迟 2-3 帧（GPU 帧延迟）后安全释放
```

#### 切换时机：FrameSync 回调

`FrameDriver::Tick()` 的执行顺序（详见 FrameDriver 源码）：

```
BeginFrame → Immediate → Render Phase → Update Phase → FrameSync → EndFrame
```

**FrameSync**（帧同步回调）是原子堆切换的自然挂钩点，原因：

1. FrameSync 是 Update Phase 与下一帧 Render Phase 之间的**数据冻结点**
2. 执行 FrameSync 时，本帧所有 **Update Phase 已完成**（包括 PreRender），逻辑数据就绪
3. 旧堆引用的所有命令列表已提交到 GPU 队列，在 EndFrame Present 后 GPU 会完成消费
4. FrameResourceManager 已经通过 fence 跟踪帧完成状态，旧堆可注册为"3 帧后回收"
5. FrameSync 已有的回调机制（`RegisterFrameSyncCallback` / `FrameSyncCallback`）可直接复用

切换流程在 Tick 中的具体位置：

```
Tick N:
  BeginFrame (GPU fence 同步)
  Render Phase → 使用旧堆（读取上一帧冻结的数据）
  Update Phase → 更新逻辑 + 新堆加载就绪
  FrameSync → [原子切换堆指针]    ← 新堆生效，旧堆标记待回收
  EndFrame (Present)

Tick N+1 ~ N+3:
  旧堆由 FrameResourceManager 追踪，GPU fence 完成后安全释放
```

### 3.2 以"资源容器"为单位建堆

不按渲染 Pass 建堆，而是按**模型实例**或**材质实例**建堆，优势：

- 热重载粒度更细，切换代价更低
- 后台线程可独立填充各堆，不阻塞主渲染线程
- 损坏资源只影响其所在堆，不会导致整个游戏进程崩溃
- 驱动程序错误可捕获并回退旧资源，保持稳定运行

### 3.3 发布模式的简化

```
关卡加载开始
    ↓
[渲染线程等待] ← 后台线程加载所有资源到单一大堆
    ↓
加载完成，资源全部就绪
    ↓
渲染循环开始（每帧使用单一大堆，环形分配器高效运转）
    ↓
关卡卸载触发
    ↓
等待 GPU 空闲 → 重置环形分配器指针（整个堆逻辑清空）
    ↓
下一个关卡加载...
```

渲染循环没有任何动态资源创建/销毁的开销。

---

## 4. 双向同步设计

编辑器与游戏之间实现"所见即所得"的双向影响，而非单向推送。

### 4.1 核心原则

放弃实时双向同步，改用**基于命令序列的多帧录制与回放**：

- 传输的是**意图**，而非**状态**
- 编辑器发送一段操作序列（命令序列），如：帧 1 移动物体 A 到位置 X；帧 2 修改材质参数 Z
- 游戏端接收序列，按时间戳在本地逐帧重演
- 游戏端回传实际渲染结果或性能数据，用于验证

### 4.2 通信方向

| 方向 | 内容 | 用途 |
|------|------|------|
| **下行**（编辑器 → 游戏） | 资产修改操作、摄像机关键帧、动画曲线、物理参数调整 | 驱动游戏进程进行渲染或动画预览 |
| **上行**（游戏 → 编辑器） | 实际渲染帧（缩略图）、性能 Profile 数据、碰撞检测结果 | 编辑器的属性面板显示真实数值，用于调试 |

### 4.3 避免同步风暴

双向同步最核心的风险是循环更新。解决方案：

1. **赋予每帧唯一 ID**：所有操作挂载到对应帧 ID 下
2. **双方只同步"输入"**：游戏端同步的是物理引擎的输入（如"第 100 帧施加向右的力"），而非最终位置
3. **最终一致性**：只要输入序列一致，即使帧率不同，最终状态收敛

### 4.4 命令类型区分

在命令序列中区分两种命令：

- **Game → Editor 命令**：世界状态变更（位置、血量、开关状态），编辑器收到后更新场景图
- **Editor → Game 命令**：资产配置变更（材质参数、模型替换），游戏收到后执行热重载

**冲突解决**：当双方同时修改同一属性时，以时间戳更新的为准，或通过自定义规则（如编辑器优先）解决。

### 4.5 反向优化（运行时驱动场景迭代）

双向同步的核心价值在于"运行时驱动的场景迭代"——以玩家的视角直接"雕刻"游戏世界：

| 场景 | 传统做法 | 双向同步下的反向优化 |
|------|----------|---------------------|
| 玩法验证 | 编辑器摆障碍物 → 运行测试 → 退出修改 → 再运行 | 边玩边调：操控角色时直接拖拽平台，编辑器实时同步 |
| 性能调优 | 看 Profile 数据猜问题 → 回编辑器修改 | 实时可视化反馈，运行时动态调整 LOD 距离，立刻看到帧率变化 |
| AI 寻路测试 | 生成导航网格 → 运行观察 → 反复修改 | 实时修改导航区域，AI 立刻改变路线 |

### 4.6 确定性要求

双向同步对确定性要求极高，必须确保：

- 双方使用相同的随机数种子
- 浮点数运算保持一致的精度
- 操作的执行顺序严格按帧号排序
- 每帧携带绝对时间戳（基于帧编号）
- 增量更新，只传输发生变化的属性（Delta Compressed）

---

## 5. 参考实现

- **Unreal Engine Multi-User Editing**：同步事务（Transaction）而非状态，关卡操作即时同步，资产修改保存时同步
- **GDevelop 3D 编辑器**：渲染由独立游戏引擎进程完成，编辑器通过内部消息驱动渲染进程
- **Unity EditorConnection API**：编辑器和游戏进程间双向消息通信
- **Dota 2 / Source 2 Replay 系统**：绝对时间戳 + 增量更新 + 确定性种子

---

